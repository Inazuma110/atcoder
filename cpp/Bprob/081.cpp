using namespace std;

#include <iostream>
#include <cstdlib>
#include <algorithm>
#include <cmath>
#include <limits>
#include <climits>
#include <vector>
#include <string>
#include <set>
#include <queue>
#include <stack>
#include <map>
#include <numeric>
typedef long long ll;
typedef pair<int,int> p;

#define rep(i, n) for(int (i) = 0; (i) < (n); (i)++)




int main()
{
  // input & declare
  int n;
  std::cin >> n;
  std::vector<int> v(n,0);
  bool even = true;
  for(int i : v)
  {
    cin >> i;
  }

  // process
  while (/* condition */) {
    /* code */
  }

  if (i % 2 == 1) {
    even = false;
  }
  // output


}
