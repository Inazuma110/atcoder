using namespace std;

#include <iostream>
#include <cstdlib>
#include <algorithm>
#include <cmath>
#include <limits>
#include <climits>
#include <vector>
#include <string>
#include <set>
#include <queue>
#include <stack>
#include <map>
#include <numeric>
typedef long long ll;
typedef pair<int,int> p;

#define rep(i, n) for(int (i) = 0; (i) < (n); (i)++)




int main()
{
  // input & declare
  int tmp;
  std::vector<int> v1;
  std::vector<int> v2;
  std::vector<int> v3;
  for(int i = 0; i < 3; i++){
    cin >> tmp;
    v1.push_back(tmp);
  }
  for(int i = 0; i < 3; i++){
    cin >> tmp;
    v2.push_back(tmp);
  }
  for(int i = 0; i < 3; i++){
    cin >> tmp;
    v3.push_back(tmp);
  }


  // process


  // output

}
